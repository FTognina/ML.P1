```markdown
# project script Instructions

Create a new Python file named `scripname_yourname.py` where `yourname` should be replaced with your actual name. This file will contain your script code.

A script consist of an entire pipeline that generates a resulting csv submission. In the name specify version and main method used (linear regression, lasso, ridge,...)
we will then test them and chose the best to use.
```