```markdown
# Implementation Instructions

Create a new Python file named `implementation_yourname.py` where `yourname` should be replaced with your actual name. This file will contain your implementation code.

we will then test them and chose the best to use.

functions:
mean squared error gd(y, tx, initial w,
max iters, gamma)
Linear regression using gradient descent
mean squared error sgd(y, tx, initial w,
max iters, gamma)
Linear regression using stochastic gradient descent
least squares(y, tx) Least squares regression using normal equations
ridge regression(y, tx, lambda ) Ridge regression using normal equations
logistic regression(y, tx, initial w,
max iters, gamma)
Logistic regression using gradient descent (y ∈ {0, 1})
reg logistic regression(y, tx, lambda ,
initial w, max iters, gamma)
Regularized logistic regression using gradient descent
(y ∈ {0, 1}, with regularization term λ∥w∥
2
)

```